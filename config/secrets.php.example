<?php
/**
 * Copy this file to config/secrets.php and fill in real values.
 * config/secrets.php is gitignored and must never be committed.
 */

// Admin users. Passwords are bcrypt hashes, not plaintext - generate one with:
//   php -r "echo password_hash('your-password-here', PASSWORD_DEFAULT), PHP_EOL;"
$users = [
    'admin' => [
        'password' => '$2y$12$replace.this.with.a.real.bcrypt.hash.from.the.command.above',
        'role'     => 'admin',
    ],
];

// Seconds a login stays valid before isLoggedIn() starts returning false.
$session_timeout = 3600;

// Where contact form submissions are delivered.
$to_email = 'someone@example.com';
$email_subject_prefix = '[Website Enquiry]';

// Google reCAPTCHA v3 - https://www.google.com/recaptcha/admin
$recaptcha_site_key = '';
$recaptcha_secret_key = '';
$recaptcha_v3_threshold = 0.5;
